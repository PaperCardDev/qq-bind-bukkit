name: PlayerQqBind
version: '${project.version}'
main: cn.paper_card.qq_bind.ThePlugin
api-version: 1.20
author: Paper99
folia-supported: true

depend:
  - PaperClient

commands:
  qq-bind:
    permission: qq-bind.command

permissions:
  qq-bind.command:
    default: op
